# Server
PORT=3001
NODE_ENV=development

# CORS / Domains
# Single legacy origin (kept for backwards compatibility)
CLIENT_DOMAIN=http://localhost:3000
# Comma-separated allowlist for CORS
# Example (update with your Vercel domain):
# CLIENT_ORIGINS=http://localhost:3000,https://on-the-rocks-4ieq-40x2pn5xi-drivellos-projects.vercel.app
CLIENT_ORIGINS=
# Allow any *.vercel.app (and *.vercel.sh) origin when set to "true"
ALLOW_VERCEL_WILDCARD=false
FRONT=http://localhost:3000
BACK=http://localhost:3001

# JWT / Auth
SECRET_KEY=changeme
GOOGLE_CLIENT_ID=your-google-client-id

# Database (PostgreSQL)
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_NAME=ontherocks

# Emails (Gmail App Password required)
GMAIL_APP_EMAIL=youremail@gmail.com
GMAIL_APP_PASSWORD=your-gmail-app-password

# MercadoPago
ACCESS_TOKEN_MERCADOPAGO=your-mercadopago-access-token
